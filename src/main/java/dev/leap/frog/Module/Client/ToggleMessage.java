package dev.leap.frog.Module.Client;

import dev.leap.frog.Module.Module;

public class ToggleMessage extends Module {

    public ToggleMessage() {
        super("Toggle Messages", "Sends message when module is on", Type.CLIENT);
    }
}
